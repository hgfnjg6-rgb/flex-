name: monacer
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  shared_preferences: ^2.1.1
  flutter_local_notifications: ^15.1.0+1
  # firebase_messaging: ^14.6.3
  # Temporarily disabled for web debug run due to firebase_messaging_web interop compilation issues.
  lottie: ^2.4.0
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter
  table_calendar: ^3.0.8
  http: ^1.0.0
  timezone: ^0.9.4
  uuid: ^3.0.7
  google_fonts: ^5.0.0
  flutter_animate: ^4.0.0
  awesome_notifications: ^0.10.1
  flutter_svg: ^1.1.6

  # Optional encryption helper (placeholder for secure storage)
  # encrypt: ^6.0.0  # removed placeholder to avoid dependency resolution failures; replace with a supported package if needed (e.g. flutter_secure_storage)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/mockups/hero.png
    - assets/mockups/hero2.png
    - assets/mockups/hero3.png
    - assets/icons/app_icon_512.png
    - assets/lotties/loading.json
